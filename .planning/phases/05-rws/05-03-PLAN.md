---
phase: 05-rws
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/cluster/rws/mod.rs
autonomous: true
requirements:
  - RWS-01
  - RWS-02

must_haves:
  truths:
    - "run_with_runners 在五个高代价 phase 完成后各自调用 cp.save()"
    - "重跑时已完成的 phase（cp.xxx_done == true）被跳过，打印跳过日志"
    - "run_read_routing_phase 在 run_verify_phase 之后被调用（替换 TODO:50）"
    - "全部成功后调用 ClusterCheckpoint::remove() 删除 checkpoint 文件"
    - "standby_restore_done 之后的 phase（distribute/startup/watcher/monitor/sqllog/verify/read_routing）不单独打点"
  artifacts:
    - path: "src/cluster/rws/mod.rs"
      provides: "checkpoint gate 集成 + run_read_routing_phase 调用"
      contains: "ClusterCheckpoint"
  key_links:
    - from: "rws/mod.rs::run_with_runners"
      to: "cluster::checkpoint::ClusterCheckpoint"
      via: "crate::cluster::checkpoint::ClusterCheckpoint::load()"
      pattern: "ClusterCheckpoint::load"
    - from: "rws/mod.rs::run_with_runners"
      to: "phases::run_read_routing_phase"
      via: "直接调用，替换 TODO:50"
      pattern: "run_read_routing_phase"
---

<objective>
修改 src/cluster/rws/mod.rs 的 run_with_runners 函数，嵌入五个 checkpoint gate（每个高代价 phase 完成后 save），替换 TODO:50 为 run_read_routing_phase 调用，全部完成后删除 checkpoint 文件。同时添加集成测试验证 checkpoint gate 跳过逻辑。

Purpose: 使 dm-installer install rws 支持中断重跑，不重复执行危险操作（如 dmrman restore）；同时补全 run_read_routing_phase 调用使流程端到端可走通。
Output: src/cluster/rws/mod.rs（修改）
</objective>

<execution_context>
@/Users/guang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rws/05-CONTEXT.md
@.planning/phases/05-rws/05-VALIDATION.md
@.planning/phases/05-rws/05-01-SUMMARY.md
@.planning/phases/05-rws/05-02-SUMMARY.md
</context>

<interfaces>
<!-- 从 Plan 01 和 Plan 02 创建的接口，执行前必须理解 -->

从 src/cluster/checkpoint.rs（Plan 01 输出）：

pub struct ClusterCheckpoint {
    pub preflight_done: bool,
    pub install_done: bool,
    pub primary_init_done: bool,
    pub backup_done: bool,
    pub standby_restore_done: bool,
}

impl ClusterCheckpoint {
    pub fn save(&self) -> Result<()>
    pub fn load() -> Result<Option<Self>>
    pub fn remove() -> Result<()>
}

// 使用方式：
// let mut cp = crate::cluster::checkpoint::ClusterCheckpoint::load()?.unwrap_or_default();
// cp.preflight_done = true; cp.save()?;
// crate::cluster::checkpoint::ClusterCheckpoint::remove()?;

从 src/cluster/phases.rs（Plan 02 输出）：

pub async fn run_read_routing_phase(
    specific: &ClusterSpecificConfig,
    runners: &Runners,
    dminit: &DminitConfig,
) -> Result<()>

// 调用方式（替换 TODO:50）：
// phases::run_read_routing_phase(&specific, &runners, &dminit).await?;
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 重写 run_with_runners — 嵌入 checkpoint gate + 替换 TODO</name>
  <files>src/cluster/rws/mod.rs</files>
  <read_first>
    - src/cluster/rws/mod.rs — 读取全文（当前 54 行），掌握 run_with_runners 完整签名（特别是 health_check_fn 泛型约束）、现有 phase 调用顺序（lines 39-51）、TODO:50 位置
    - src/cluster/checkpoint.rs（Plan 01 输出）— 确认 ClusterCheckpoint::load/save/remove 签名
    - src/cluster/phases.rs（Plan 02 输出）— 确认 run_read_routing_phase 签名与 D-11 一致
    - src/common/ssh/mock.rs — 确认 MockRunner 用于集成测试
  </read_first>
  <behavior>
    - Test 1 (test_checkpoint_skips_completed): 构建 ClusterCheckpoint{preflight_done:true, install_done:true, primary_init_done:true, backup_done:false, ...}，写入 TempDir；MockRunner 模拟 install_phase 和 primary_init_phase 不应被调用（strict 模式或记录调用次数）；断言 preflight 和 install 和 primary_init 三个 phase 的 runner.exec 未被调用（或调用次数为 0），而 backup_phase 对应命令被调用
    注意：test_checkpoint_skips_completed 是集成测试，需要 run_with_runners 的完整调用链。由于 common/specific 结构较复杂，测试可以选择以下简化策略之一：
    A. 仅测试 checkpoint 加载 + 条件判断逻辑（unit 测试 cp.xxx_done 分支），不调用 run_with_runners
    B. 调用 run_with_runners 但使用完整的 MockRunner 预设所有后续命令
    推荐选 A（验证 gate 逻辑本身），因为 run_with_runners 调用链过长。测试名改为 test_checkpoint_gate_skips_done_phases，验证：given preflight_done=true, install_done=true 的 checkpoint 文件存在，load 后 if !cp.preflight_done 分支不进入（通过断言 loaded.preflight_done == true 而非调用 run_with_runners）
  </behavior>
  <action>
    重写 src/cluster/rws/mod.rs 的 run_with_runners 函数体（保持函数签名不变）：

    函数体开头：
      let dminit = specific.dminit.clone();
      let mut cp = crate::cluster::checkpoint::ClusterCheckpoint::load()?.unwrap_or_default();（per D-01：从当前工作目录加载）

    五个 checkpoint gate（per D-02）：

    gate 1（preflight，per D-02）：
      if !cp.preflight_done {
        phases::run_preflight(&runners, &dminit).await?;
        cp.preflight_done = true;
        cp.save()?;
      } else {
        tracing::info!("[续] 跳过预检查（checkpoint）");
      }

    gate 2（install，per D-02）：
      if !cp.install_done {
        phases::run_install_phase(&common, &runners, &dminit).await?;
        cp.install_done = true;
        cp.save()?;
      } else {
        tracing::info!("[续] 跳过安装（checkpoint）");
      }

    gate 3（primary_init，per D-02）：
      if !cp.primary_init_done {
        phases::run_primary_init_phase(&runners, &health_check_fn, &dminit).await?;
        cp.primary_init_done = true;
        cp.save()?;
      } else {
        tracing::info!("[续] 跳过主节点初始化（checkpoint）");
      }

    gate 4（backup，per D-02）：
      if !cp.backup_done {
        phases::run_backup_phase(&runners, &dminit).await?;
        cp.backup_done = true;
        cp.save()?;
      } else {
        tracing::info!("[续] 跳过备份（checkpoint）");
      }

    gate 5（standby_restore，per D-02）：
      if !cp.standby_restore_done {
        phases::run_standby_restore_phase(&runners, &dminit).await?;
        cp.standby_restore_done = true;
        cp.save()?;
      } else {
        tracing::info!("[续] 跳过备节点还原（checkpoint）");
      }

    standby_restore_done 之后不打点（per D-03），直接执行：
      phases::run_distribute_phase(&specific, &runners, &dminit).await?;
      phases::run_startup_phase(&specific, &runners, &health_check_fn, &dminit).await?;
      phases::run_watcher_phase(&runners, &dminit).await?;
      phases::run_monitor_phase(&specific, &runners, &dminit).await?;
      phases::run_sqllog_phase(&specific, &runners, &dminit).await?;
      phases::run_verify_phase(&runners, &dminit).await?;
      phases::run_read_routing_phase(&specific, &runners, &dminit).await?;（per D-13：替换 TODO:50）

    完成后删除 checkpoint（per D-04）：
      crate::cluster::checkpoint::ClusterCheckpoint::remove()?;
      tracing::info!("集群部署完成");
      Ok(())

    函数长度评估：五个 gate 各约 6 行 = 30 行，后续 7 个直接调用 = 7 行，头尾约 5 行，合计约 42 行。
    如超过 40 行，提取辅助函数 run_checkpointed_phases(cp: &mut ClusterCheckpoint, common: &CommonConfig, runners: &Runners, health_check_fn: ..., dminit: &DminitConfig) -> Result<()>，将五个 gate 移入，主函数调用 run_checkpointed_phases(&mut cp, ...).await?。

    #[cfg(test)] mod tests（在文件末尾添加）：
      use tempfile::TempDir;
      use super::*;

      #[test]
      fn test_checkpoint_gate_skips_done_phases()：
        let dir = TempDir::new().unwrap();
        let cp = crate::cluster::checkpoint::ClusterCheckpoint {
          preflight_done: true,
          install_done: true,
          primary_init_done: true,
          backup_done: false,
          standby_restore_done: false,
        };
        cp.save_to(dir.path()).unwrap();（注意：需要引入 save_to pub(crate) 或通过 std::env::set_current_dir 设置 cwd）
        let loaded = crate::cluster::checkpoint::ClusterCheckpoint::load_from(dir.path()).unwrap().unwrap();
        assert!(loaded.preflight_done, "preflight gate: 应可跳过");
        assert!(loaded.install_done, "install gate: 应可跳过");
        assert!(loaded.primary_init_done, "primary_init gate: 应可跳过");
        assert!(!loaded.backup_done, "backup gate: 应继续执行");
        assert!(!loaded.standby_restore_done, "standby_restore gate: 应继续执行");
  </action>
  <verify>
    <automated>cargo test -p dm-installer cluster::rws -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>
    - src/cluster/rws/mod.rs 含 ClusterCheckpoint::load 调用和五个 checkpoint gate
    - TODO:50 已被 phases::run_read_routing_phase(&specific, &runners, &dminit).await? 替换
    - 全部完成后含 ClusterCheckpoint::remove()? 调用
    - cargo test cluster::rws::tests 退出码 0，test_checkpoint_gate_skips_done_phases PASSED
    - cargo build -p dm-installer 退出码 0
    - grep 确认：grep "run_read_routing_phase" src/cluster/rws/mod.rs 输出非空
    - grep 确认：grep "TODO" src/cluster/rws/mod.rs 输出为空（TODO 已删除）
    - grep 确认：grep "ClusterCheckpoint::remove" src/cluster/rws/mod.rs 输出非空
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 控制机文件系统 → checkpoint 文件 | run_with_runners 读写 dm_cluster_checkpoint.json |
| 控制机 → 各节点 SSH | runners 传入，phase 函数通过 runner.exec 执行远端命令 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | checkpoint gate 跳过逻辑 | accept | 用户可手动删除 dm_cluster_checkpoint.json 强制全量重跑；gate 仅跳过已完成步骤，不降低安全性 |
| T-05-08 | Denial of Service | cp.save() 失败（权限/磁盘满） | accept | save 失败返回 Err，中止部署；用户解决磁盘问题后可重跑（此时 checkpoint 未保存，会重跑该 phase，与 standalone 相同权衡，per Pitfall 3） |
| T-05-09 | Elevation of Privilege | run_with_runners 顺序执行 | accept | phase 调用顺序与原有相同，仅新增 gate 条件判断；无权限提升风险 |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 本 phase 无新依赖，slopcheck 不适用 |
</threat_model>

<verification>
cargo test -p dm-installer cluster::rws -- --nocapture
cargo build -p dm-installer
grep "run_read_routing_phase" src/cluster/rws/mod.rs
grep "TODO" src/cluster/rws/mod.rs
grep "ClusterCheckpoint::remove" src/cluster/rws/mod.rs
cargo test -p dm-installer -- --nocapture 2>&1 | grep -E "PASSED|FAILED|test result"
</verification>

<success_criteria>
- src/cluster/rws/mod.rs 的 run_with_runners 含五个 checkpoint gate（preflight/install/primary_init/backup/standby_restore）
- run_read_routing_phase 在 run_verify_phase 之后被调用（per D-13），TODO:50 注释已删除
- ClusterCheckpoint::remove()? 在最后一行 tracing::info!("集群部署完成") 之前调用（per D-04）
- cargo test cluster::rws::tests::test_checkpoint_gate_skips_done_phases PASSED
- cargo test -p dm-installer 全套退出码 0（无 FAILED）
- cargo build -p dm-installer 退出码 0
</success_criteria>

<output>
创建 .planning/phases/05-rws/05-03-SUMMARY.md，记录：run_with_runners 实际行数（是否需要提取辅助函数）、checkpoint gate 集成结果、TODO 删除确认、完整测试套件状态
</output>
