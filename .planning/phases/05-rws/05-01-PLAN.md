---
phase: 05-rws
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cluster/checkpoint.rs
  - src/cluster/mod.rs
autonomous: true
requirements:
  - RWS-01

must_haves:
  truths:
    - "ClusterCheckpoint 可将布尔标志序列化为 JSON 并从磁盘还原"
    - "损坏或缺失的 checkpoint 文件不导致崩溃，返回 None"
    - "remove() 调用后文件不存在"
    - "save/load/remove 三个公共代理方法指向当前工作目录"
    - "src/cluster/mod.rs 中已声明 pub mod checkpoint"
  artifacts:
    - path: "src/cluster/checkpoint.rs"
      provides: "ClusterCheckpoint 结构体 + save/load/remove 方法 + 内嵌测试"
      contains: "struct ClusterCheckpoint"
    - path: "src/cluster/mod.rs"
      provides: "模块声明入口"
      contains: "pub mod checkpoint"
  key_links:
    - from: "src/cluster/rws/mod.rs"
      to: "src/cluster/checkpoint.rs"
      via: "crate::cluster::checkpoint::ClusterCheckpoint::load()"
      pattern: "cluster::checkpoint"
---

<objective>
新建集群 checkpoint 文件，模仿 src/standalone/checkpoint.rs 的结构，记录五个高代价 phase 的布尔完成标志，支持中断重跑。同时在 src/cluster/mod.rs 添加模块声明使其可被 rws/mod.rs 引用。

Purpose: 用户在主备集群安装中途中断后重跑时，可从上次已完成的 phase 继续，避免重复执行危险操作（如 dmrman restore）。
Output: src/cluster/checkpoint.rs（新建）、src/cluster/mod.rs（追加一行）
</objective>

<execution_context>
@/Users/guang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rws/05-CONTEXT.md
@.planning/phases/05-rws/05-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 src/cluster/checkpoint.rs 并声明模块</name>
  <files>src/cluster/checkpoint.rs, src/cluster/mod.rs</files>
  <read_first>
    - src/standalone/checkpoint.rs — 完整参考实现（结构体 derive、save_to/load_from/remove_from 方法、cwd() 辅助函数、#[cfg(test)] 模块），逐行对照；
    - src/cluster/mod.rs — 确认现有 pub mod 列表，在正确位置追加 pub mod checkpoint;
  </read_first>
  <behavior>
    - Test 1 (test_roundtrip): 用 TempDir 调用 save_to，再 load_from，断言 preflight_done/install_done/primary_init_done/backup_done/standby_restore_done 值与写入一致
    - Test 2 (test_load_returns_none): 空 TempDir 调用 load_from，断言返回 Ok(None)
    - Test 3 (test_remove_deletes_file): save_to 后 remove_from，断言文件不存在（dir.path().join("dm_cluster_checkpoint.json").exists() == false）
    - Test 4 (test_load_ignores_corrupt): 向 TempDir 写入 "not json" 后 load_from，断言返回 Ok(None)（不 panic，不 Err）
  </behavior>
  <action>
    在 src/cluster/checkpoint.rs 中创建以下内容（不含 fenced code block，以下为标识符列表）：

    导入：anyhow::Result, serde::{Deserialize, Serialize}, std::path::{Path, PathBuf}

    常量：const FILE_NAME: &str = "dm_cluster_checkpoint.json"

    结构体：pub struct ClusterCheckpoint，derive(Debug, Clone, Serialize, Deserialize, Default)，五个字段全为 pub bool，全部加 #[serde(default)]：
      - preflight_done: bool（per D-02）
      - install_done: bool（per D-02）
      - primary_init_done: bool（per D-02）
      - backup_done: bool（per D-02）
      - standby_restore_done: bool（per D-02）

    impl ClusterCheckpoint：
      - pub fn save(&self) -> Result<()>: 调用 self.save_to(&cwd())
      - pub fn load() -> Result<Option<Self>>: 调用 Self::load_from(&cwd())（per D-01：当前工作目录代理）
      - pub fn remove() -> Result<()>: 调用 Self::remove_from(&cwd())（per D-04）
      - pub(crate) fn save_to(&self, dir: &Path) -> Result<()>: serde_json::to_string_pretty(self)? + fs::write(dir.join(FILE_NAME), content)? + tracing::debug!("检查点已保存: {}", path.display())
      - pub(crate) fn load_from(dir: &Path) -> Result<Option<Self>>: 检查 path.exists()；fs::read_to_string；serde_json::from_str，Err 分支打 tracing::warn!("检查点文件格式错误，忽略: {}", e) 后 return Ok(None)；成功时 println!("[续] 检测到检查点，从上次进度继续安装") 后 return Ok(Some(cp))
      - pub(crate) fn remove_from(dir: &Path) -> Result<()>: if path.exists() { fs::remove_file(&path)?; tracing::debug!("检查点已删除") }

    私有辅助：fn cwd() -> PathBuf: std::env::current_dir().unwrap_or_else(|_| PathBuf::from("."))

    #[cfg(test)] mod tests：use super::*; use tempfile::TempDir; 实现上述四个测试（test_roundtrip/test_load_returns_none/test_remove_deletes_file/test_load_ignores_corrupt）

    注意：ClusterCheckpoint 与 standalone::Checkpoint 关键差异：
    - 无 install_path 匹配键（per A-02，rws.toml 和 checkpoint 同目录不会混淆）
    - 无 new() 构造函数（用 Default derive + unwrap_or_default()）
    - load_from 签名为 load_from(dir: &Path)，无第二参数
    - 不调用 configure_read_only_standby（per D-06，反模式）

    在 src/cluster/mod.rs 的现有 pub mod 列表中，按字母序在 pub mod deploy; 之后插入：
      pub mod checkpoint;
  </action>
  <verify>
    <automated>cargo test -p dm-installer cluster::checkpoint::tests -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>
    - src/cluster/checkpoint.rs 存在，含 struct ClusterCheckpoint 和五个布尔字段
    - src/cluster/mod.rs 含 pub mod checkpoint;
    - cargo test -p dm-installer cluster::checkpoint::tests 退出码 0，4 个测试全部 PASSED
    - cargo build -p dm-installer 退出码 0（无编译错误）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 控制机文件系统 → checkpoint 文件 | checkpoint 文件由安装器自身写入，不接受外部用户输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | dm_cluster_checkpoint.json | accept | 文件仅含布尔标志，无敏感数据；篡改最多导致跳过某步骤，用户可手动删除强制重跑 |
| T-05-02 | Information Disclosure | dm_cluster_checkpoint.json | accept | 无密码、无 SSH 凭据、无路径信息；纯布尔标志 |
| T-05-03 | Denial of Service | load_from corrupt file | mitigate | serde_json::from_str Err 分支静默 warn + return Ok(None)，不崩溃（test_load_ignores_corrupt 验证） |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 本 phase 无新依赖，slopcheck 不适用 |
</threat_model>

<verification>
cargo test -p dm-installer cluster::checkpoint -- --nocapture
cargo build -p dm-installer
grep -c "pub mod checkpoint" src/cluster/mod.rs
grep -c "struct ClusterCheckpoint" src/cluster/checkpoint.rs
</verification>

<success_criteria>
- src/cluster/checkpoint.rs 存在，含 ClusterCheckpoint 结构体和 5 个布尔字段（preflight_done/install_done/primary_init_done/backup_done/standby_restore_done）
- src/cluster/mod.rs 含 pub mod checkpoint; 声明
- cargo test cluster::checkpoint::tests 全部 PASSED（4 个测试：roundtrip/none_when_missing/remove_deletes/ignores_corrupt）
- cargo build 退出码 0
</success_criteria>

<output>
创建 .planning/phases/05-rws/05-01-SUMMARY.md，记录：文件创建情况、实际结构体字段、测试通过情况、与 standalone checkpoint 的差异点
</output>
