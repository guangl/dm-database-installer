---
phase: 05-rws
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cluster/phases.rs
autonomous: true
requirements:
  - RWS-01
  - RWS-02

must_haves:
  truths:
    - "run_read_routing_phase 遍历 runners，找 role==Standby && read_only==true 的节点"
    - "对每个只读备节点调用 wait_for_standby_open，最多轮询 24 次（间隔 5s）"
    - "当 disql 输出同时包含 OPEN 和 STANDBY 时返回 Ok(())"
    - "当 24 次重试后未达到 OPEN 状态时返回 Err 含节点 host 信息"
    - "当无 read_only=true 节点时直接返回 Ok(()) 并打 warn 日志"
    - "函数体均不超过 40 行"
  artifacts:
    - path: "src/cluster/phases.rs"
      provides: "run_read_routing_phase + wait_for_standby_open 两个函数"
      contains: "run_read_routing_phase"
  key_links:
    - from: "run_read_routing_phase"
      to: "wait_for_standby_open"
      via: "串行 for 循环调用"
      pattern: "wait_for_standby_open"
    - from: "wait_for_standby_open"
      to: "runner.exec"
      via: "disql V$INSTANCE 查询"
      pattern: "SELECT STATUS\\$,MODE\\$ FROM V\\$INSTANCE"
---

<objective>
在 src/cluster/phases.rs 末尾追加两个函数：wait_for_standby_open（轮询辅助）和 run_read_routing_phase（公开 phase 入口）。实现对 read_only=true 备节点的等待验证，确保 dmwatcher 启动后备库自动达到 STATUS$=OPEN MODE$=STANDBY 状态。

Purpose: 补全 rws/mod.rs 中的 TODO:50，使 dm-installer install rws 端到端可走通（RWS-01/RWS-02）。
Output: src/cluster/phases.rs 追加两函数 + 内嵌测试 3 个
</objective>

<execution_context>
@/Users/guang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rws/05-CONTEXT.md
@.planning/phases/05-rws/05-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 追加 wait_for_standby_open 轮询辅助函数（含测试）</name>
  <files>src/cluster/phases.rs</files>
  <read_first>
    - src/cluster/phases.rs — 读取全文，掌握现有 imports（use crate::cluster::deploy 等）、Runners 类型别名、run_verify_phase 的 filter+map 模式、文件末尾位置
    - src/cluster/deploy.rs lines 391-436 — verify_node_role 的 disql cmd 构建方式（shell_quote 用法、format! 模板）
    - src/cluster/deploy.rs lines 486-507 — configure_sqllog retry loop 的 for attempt in 1..=N 模式、tokio::time::sleep 调用
    - src/common/ssh/mock.rs — MockRunner::new 签名和 tuple 预设格式（(String, i32, Vec<u8>)）
  </read_first>
  <behavior>
    - Test 1 (test_run_read_routing_phase_success): 构建含一个 Standby+read_only=true 节点的 runners，MockRunner 预设 disql 命令前缀匹配（含 "SELECT STATUS$,MODE$"），返回 (0, b"STATUS$   MODE$\nOPEN      STANDBY\n".to_vec())；调用 run_read_routing_phase；断言返回 Ok(())
    - Test 2 (test_run_read_routing_phase_timeout): 构建同样 runners，MockRunner 预设返回 (0, b"STATUS$   MODE$\nMOUNT     STANDBY\n".to_vec())（永远不含 OPEN）；断言 run_read_routing_phase 返回 Err，Err 消息含节点 host
    - Test 3 (test_run_read_routing_phase_no_readonly): 构建含 Standby 节点但 read_only=false 的 runners；断言 run_read_routing_phase 返回 Ok(())（跳过验证）
    注意 timeout 测试：MockRunner 返回 MOUNT 而非空，MAX_RETRIES 必须提取为常量，测试中可在模块内直接访问以验证行为（不需要注入参数——MockRunner 每次都返回 MOUNT，N 次后超时退出，N=24 在 CI 中等待 120s；为避免 CI 超时，将 MAX_RETRIES/POLL_INTERVAL_SECS 提取为 pub(crate) const，测试覆盖 happy path 即可，timeout 测试用 MAX_RETRIES=1 的变体或在 wait_for_standby_open_with 中暴露参数）
  </behavior>
  <action>
    在 src/cluster/phases.rs 文件末尾（run_verify_phase 之后）追加以下内容（标识符描述，不含代码块）：

    常量（在函数外定义，per RESEARCH.md Open Question 1）：
      - const MAX_RETRIES: u32 = 24（per D-09：最多 24 次）
      - const POLL_INTERVAL_SECS: u64 = 5（per D-09：5s 间隔）

    私有辅助函数 wait_for_standby_open(node: &NodeConfig, dminit: &DminitConfig, runner: &dyn ssh::CommandRunner) -> Result<()>：
      - 使用 use crate::common::shell_quote;（与 deploy.rs line 12 相同 import 路径）
      - cmd 构建：format!("echo 'SELECT STATUS$,MODE$ FROM V$INSTANCE;' | {}/bin/disql SYSDBA/SYSDBA@localhost:{}", shell_quote(&dminit.install_path), dminit.port)（与 deploy.rs:402-405 完全相同格式，per RESEARCH.md 防注入模式）
      - for attempt in 1..=MAX_RETRIES 循环：
        * runner.exec(&cmd).await .map_err(|e| anyhow::anyhow!("poll V$INSTANCE 失败: {}", e))?
        * 若 exit_code == 0 且 output.contains("OPEN") && output.contains("STANDBY") → tracing::info!("[node:{:?}] 只读备库已就绪 STATUS$=OPEN MODE$=STANDBY", node.role) + return Ok(())
        * 若 attempt < MAX_RETRIES → tracing::warn!("[node:{:?}] 备库尚未 OPEN（{}/{}），{}s 后重试", node.role, attempt, MAX_RETRIES, POLL_INTERVAL_SECS) + tokio::time::sleep(std::time::Duration::from_secs(POLL_INTERVAL_SECS)).await
      - 循环结束后：anyhow::bail!("备节点 {} 未在 {}s 内达到 STATUS$=OPEN MODE$=STANDBY", node.host, MAX_RETRIES as u64 * POLL_INTERVAL_SECS)

    函数长度约束检查：wait_for_standby_open 约 22 行，满足 ≤40 行（CLAUDE.md）

    注意反模式（per RESEARCH.md Anti-Patterns）：
    - 不调用 deploy::configure_read_only_standby()（per D-06）
    - 不用 deploy::verify_node_role 替代（其不检查 STATUS$=OPEN，无重试，per Pitfall 1）
    - wait_for_standby_open 内不调用 cp.save()（per D-03，checkpoint 在 rws/mod.rs 管理）

    #[cfg(test)] mod tests（追加到文件末尾测试模块或新建）：
      use super::*;
      use crate::common::ssh::mock::MockRunner;
      use crate::config::cluster::{DminitConfig, NodeConfig, NodeRole, SshCredentials};

      测试辅助 fn make_standby_node(host: &str, read_only: bool) -> NodeConfig：构建 role=Standby 节点
      测试辅助 fn make_dminit() -> DminitConfig：install_path="/opt/dmdbms", port=5236

      三个 #[tokio::test] 函数对应上述三个 behavior 用例
      timeout 测试使用预设永远返回 MOUNT 的 MockRunner（每次 exec 返回含 "MOUNT" 的输出），同时将 MAX_RETRIES 在测试中重新定义为 2 以控制等待时间（或使用 wait_for_standby_open 调用并接受短暂等待）
      替代方案：提取内部测试专用函数 wait_for_standby_open_impl(node, dminit, runner, max_retries, interval_secs)，公开函数用 (MAX_RETRIES, POLL_INTERVAL_SECS) 调用，测试用 (2, 0) 调用（0s 间隔，快速失败）
  </action>
  <verify>
    <automated>cargo test -p dm-installer cluster::phases::tests -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <done>
    - src/cluster/phases.rs 末尾含 wait_for_standby_open 和 run_read_routing_phase 两个函数
    - 每个函数体 ≤40 行（cargo fmt 后行计数）
    - cargo test cluster::phases::tests 退出码 0，3 个新测试全部 PASSED
    - cargo build -p dm-installer 退出码 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 追加 run_read_routing_phase 公开入口函数</name>
  <files>src/cluster/phases.rs</files>
  <read_first>
    - src/cluster/phases.rs — 读取 Task 1 完成后的文件，确认 wait_for_standby_open 已存在，确认 run_verify_phase 的签名模式（现有函数的 &ClusterSpecificConfig 参数用法）
    - src/cluster/deploy.rs lines 391-436 — 确认 NodeRole 导入路径：use crate::config::cluster::NodeRole
    - src/config/cluster.rs lines 186-210 — 确认 NodeConfig.read_only: bool 字段名
  </read_first>
  <behavior>
    - 已覆盖：test_run_read_routing_phase_success 和 test_run_read_routing_phase_no_readonly（在 Task 1 中定义，Task 2 通过同一测试套件验证）
  </behavior>
  <action>
    在 wait_for_standby_open 之后插入公开函数 run_read_routing_phase（per D-10/D-11）：

    签名（per D-11，不可更改）：
      pub async fn run_read_routing_phase(specific: &ClusterSpecificConfig, runners: &Runners, dminit: &DminitConfig) -> Result<()>

    函数体：
      use crate::config::cluster::NodeRole;
      tracing::info!("[cluster][12/12] 等待只读备库进入 OPEN 状态")（per D-13 [12/12] 标记）

      let readonly_standbys: Vec<_> = runners.iter()
        .filter(|(node, _)| node.role == NodeRole::Standby && node.read_only)
        .collect();（per D-12：role==Standby && read_only==true）

      if readonly_standbys.is_empty() {
        tracing::warn!("[cluster][12/12] 无 read_only=true 的备节点，跳过只读验证");
        return Ok(());
      }（per D-12：无节点时跳过）

      for (node, runner) in &readonly_standbys {
        wait_for_standby_open(node, dminit, runner.as_ref()).await?;（per D-12：串行等待）
      }

      tracing::info!("[cluster][12/12] 所有只读备库就绪");
      Ok(())

    注意：specific 参数在当前实现中未使用，但签名按 D-11 必须包含（per A-04：保持与其他 phase 函数一致性，future-proof），Rust 编译器会给出 unused 警告，可加 let _ = specific; 或 #[allow(unused_variables)] 压制

    函数长度：约 15 行，满足 ≤40 行约束
  </action>
  <verify>
    <automated>cargo test -p dm-installer cluster::phases -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <done>
    - src/cluster/phases.rs 含 pub async fn run_read_routing_phase 函数，签名与 D-11 一致
    - cargo test cluster::phases::tests 退出码 0，包含 test_run_read_routing_phase_success / test_run_read_routing_phase_timeout / test_run_read_routing_phase_no_readonly 三个测试全 PASSED
    - cargo build -p dm-installer 退出码 0（无 unused_variables 错误，允许 warning）
    - grep 确认签名：grep "pub async fn run_read_routing_phase" src/cluster/phases.rs 输出非空
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 控制机 → 备节点 SSH | disql 命令通过 runner.exec() 发送到远端，命令包含 install_path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | disql 命令构建（wait_for_standby_open） | mitigate | shell_quote(&dminit.install_path) 防止路径注入（与 deploy.rs 现有模式一致，per RESEARCH.md Shared Patterns） |
| T-05-05 | Denial of Service | 轮询循环（最多 24 次 × 5s） | accept | 硬编码 MAX_RETRIES=24，120s 后强制 bail!；不存在无限等待风险 |
| T-05-06 | Information Disclosure | disql 输出（stdout） | accept | 输出仅含 V$INSTANCE 元数据（STATUS$/MODE$），无密码或用户数据 |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | 本 phase 无新依赖，slopcheck 不适用 |
</threat_model>

<verification>
cargo test -p dm-installer cluster::phases::tests -- --nocapture
cargo build -p dm-installer
grep "pub async fn run_read_routing_phase" src/cluster/phases.rs
grep "async fn wait_for_standby_open" src/cluster/phases.rs
grep -v '^[[:space:]]*//' src/cluster/phases.rs | grep -c "fn run_read_routing_phase\|fn wait_for_standby_open"
</verification>

<success_criteria>
- src/cluster/phases.rs 含 wait_for_standby_open 和 run_read_routing_phase 两个函数
- run_read_routing_phase 签名与 D-11 完全一致（含 specific: &ClusterSpecificConfig 参数）
- 使用 [cluster][12/12] tracing 标记（per D-13）
- 不含 configure_read_only_standby 调用（per D-06 反模式）
- 三个测试全部 PASSED：success/timeout/no_readonly
- cargo build 退出码 0
</success_criteria>

<output>
创建 .planning/phases/05-rws/05-02-SUMMARY.md，记录：两个函数的实际行数、MAX_RETRIES 常量值、timeout 测试采用的策略（参数化 or 2 次重试）、任何与 RESEARCH.md 建议的差异
</output>
