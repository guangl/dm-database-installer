---
context: phase
phase: 01-curl-sh
task: 5
total_tasks: 8
status: in_progress
last_updated: 2026-06-12T06:33:18Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.

- [ ] CONSTRAINT: package.dameng.com 不可直接访问 — 该域名在境外走 TUN 代理，curl/Python/Swift 的 TLS 握手一律失败。**不要尝试用 curl/Python 直接访问 package.dameng.com**，只有 Safari（通过 Surge/ClashX TUN）能访问。探测方案改为对 download.dameng.com 做 HTTP HEAD 请求。

**Do not proceed until all boxes are checked.**

<current_state>
Phase 01 工作基本完成，只剩验证和 commit。

已完成：
- install.sh 重写为 curl|sh 风格，无参数，从 versions.json 读链接
- scripts/update-versions.sh：探测 download.dameng.com/eco/adapter/DM8/ 各平台最新 zip 链接
- .github/workflows/update-versions.yml：每日定时触发（需国内 self-hosted runner）
- 通过 AppleScript+Safari Vue 实例拿到完整 CPU/OS 映射（16 个平台组合）
- versions.json 已有 5 个已验证平台

当前卡点：其余 10+ 个平台（飞腾/龙芯/申威/海光）的文件后缀是推断的，需运行脚本验证。
</current_state>

<completed_work>

- install.sh：无参数，检测 uname -m，读 versions.json，下载 zip，解压 DMInstall.bin，静默安装，dminit，注册 systemd 服务
- scripts/update-versions.sh：纯 shell，探测 120 天内各平台 zip 链接，写入 versions.json
- .github/workflows/update-versions.yml：daily cron，需 [self-hosted, cn-runner]
- 完整 CPU/OS 映射（通过 AppleScript+Vue）：
  - X86(0): Win_64(7), rhel7(10), rhel6(8), Win_32(65), CentOS7(105), 麒麟10 SP3(106), Ubuntu22(107)
  - 飞腾2000(3): 麒麟10 SP1(25)
  - 鲲鹏920(4): 统信 Uos20(15), 麒麟10 SP1(25)
  - 龙芯4000(5): 麒麟10(14)
  - 龙芯5000(41): 统信 Uos20(15), 麒麟10(14)
  - 申威3231(98): 麒麟10(14)
  - 海光(6): 中科方德(9), 麒麟10(14), 统信 Uos20(15)
  - Docker 镜像(35)
- versions.json 初始内容（5 个已验证平台）

已验证文件后缀：
- x86_rh7_64, x86_rh6_64, x86_CentOS7_64, x86_win_64（全在 202605）
- HWarm920_kylin10_sp1_64（在 202604）
</completed_work>

<remaining_work>

- Task 5：运行 `bash scripts/update-versions.sh`，看哪些新平台能被探测到
- Task 6：修正 PLATFORMS 列表中推断错误的后缀（去掉找不到的别名，添加正确的）
- Task 7：在 install.sh 第 16 行填入实际仓库 raw URL（VERSIONS_URL）
- Task 8：commit 全部新文件（install.sh, scripts/, .github/, versions.json, .gitignore）
</remaining_work>

<decisions_made>

- install.sh 从 versions.json 读链接，不直接调 package.dameng.com API（后者境外不可达）
- update-versions.sh 用 download.dameng.com HEAD 探测代替 API（全球可达）
- x86_rh7_64 作为 x86_64 首选（最广兼容），HWarm920_kylin10_sp1_64 作为 aarch64 首选
- GitHub Actions workflow 需 self-hosted cn-runner（记录在 yml 注释里）
- 文件格式：zip（不是 iso），路径：`/eco/adapter/DM8/{YYYYMM}/dm8_{YYYYMMDD}_{suffix}.zip`
</decisions_made>

<blockers>

- 飞腾/龙芯/申威/海光文件后缀未验证（推断值），需实际探测才能确认
- package.dameng.com 仅 Safari 可访问（TUN 代理），故无法从代码直接调用下载 URL API
</blockers>

## Required Reading (in order)

1. `scripts/update-versions.sh` — 当前探测逻辑和 PLATFORMS 候选列表
2. `install.sh` — 安装流程（读 versions.json → 按架构选 URL → 下载安装）
3. `versions.json` — 当前已验证的 5 个平台

## Critical Anti-Patterns (do NOT repeat these)

- ANTI-PATTERN: 用 curl/Python/Swift 访问 package.dameng.com → 必然失败（TLS 握手失败）。只用 download.dameng.com 做 HEAD 探测，或用 AppleScript 控制 Safari。
- ANTI-PATTERN: 在 Shell for 循环里用 `declare -A` → macOS bash 3.2 不支持关联数组，改用 Python 或冒号分隔字符串。

## Infrastructure State

- versions.json: 有 5 个已验证平台，其余平台待探测填入
- VERSIONS_URL in install.sh: 需填入实际仓库 raw URL（当前为占位符）
- GitHub Actions: yml 已写好，需在仓库 Settings 配置 cn-runner label

<context>
本次 session 主要目标是把 install.sh 改成真正的 curl|sh 风格（无参数，自动从官网获取链接）。

关键发现：
1. download.dameng.com 全球可达，package.dameng.com 仅中国境内可达
2. 下载文件格式：zip，路径规律 /eco/adapter/DM8/{YYYYMM}/dm8_{date}_{platform}_64.zip
3. 平台文件后缀命名：{CPU厂商缩写}_{OS名}_64（X86 例外，直接用 x86）
4. 通过 AppleScript 读 Safari 页面 Vue 实例成功拿到完整 CPU/OS 映射

还有 10+ 个平台的后缀是根据已知 5 个推断的，可能部分猜错了。
</context>

<next_action>
运行 `bash scripts/update-versions.sh 2>&1 | tee /tmp/probe.log`，等待结果（约 3-5 分钟），根据 OK/未找到 结果修正 PLATFORMS 列表，然后 commit 全部文件。
</next_action>
