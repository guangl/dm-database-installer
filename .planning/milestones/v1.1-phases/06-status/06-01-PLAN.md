---
phase: 06-status
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli.rs
  - src/main.rs
  - src/status/mod.rs
  - Cargo.toml
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
tags:
  - cli
  - status
  - ssh
  - cluster

must_haves:
  truths:
    - "用户在终端执行 `dm-installer status` 立即看到一行对齐表头（含 Node/Host/Process/Port/Role 五列）"
    - "用户在当前目录无 config.toml 时执行 status 仍能看到本地行（local + localhost + running/stopped + listening/closed + PRIMARY/STANDBY/OPEN/unknown），不报错不退出非 0"
    - "用户在当前目录有 cluster config.toml 时，输出含本地行 + 所有 config.toml 中 nodes 的行"
    - "某个远程节点 SSH 连接超时或失败时，该行 Process/Port 为 '—'，Role 列含 'ERROR: <原因>'，其余节点行正常输出，命令退出码 0"
    - "运行中实例的 Role 列基于 disql `SELECT STATUS$,MODE$ FROM V$INSTANCE` 输出解析为 'PRIMARY' / 'STANDBY' / 'OPEN' / 'unknown'"
    - "`cargo test status` 全绿（包含本地检测、远程查询、表格格式化、错误降级、disql 输出解析的单元测试）"
  artifacts:
    - path: "src/cli.rs"
      provides: "Commands::Status(StatusArgs) 枚举变体 + StatusArgs 结构（无字段）"
      contains: "Status(StatusArgs)"
    - path: "src/main.rs"
      provides: "mod status 声明 + Commands::Status 分支 dispatch 到 status::run(args).await"
      contains: "mod status"
    - path: "src/status/mod.rs"
      provides: "pub async fn run(args: &StatusArgs) -> Result<()> 顶层入口 + NodeStatus 结构 + 本地/远程查询 + 表格格式化 + 单元测试"
      min_lines: 200
    - path: "Cargo.toml"
      provides: "futures = 0.3 已在 dependencies（已有依赖确认）"
      contains: "futures"
  key_links:
    - from: "src/main.rs"
      to: "src/status/mod.rs"
      via: "cli::Commands::Status(args) => status::run(args).await"
      pattern: "Commands::Status.*status::run"
    - from: "src/status/mod.rs"
      to: "src/common/ssh/session.rs"
      via: "SshSession::connect(host, 22, &node.ssh)"
      pattern: "SshSession::connect"
    - from: "src/status/mod.rs"
      to: "src/common/ssh/runner.rs"
      via: "CommandRunner::exec(&cmd).await"
      pattern: "runner.exec"
    - from: "src/status/mod.rs"
      to: "src/config/mod.rs"
      via: "config::load_config().ok() — 失败降级为 None"
      pattern: "config::load_config"
    - from: "src/status/mod.rs"
      to: "src/common/mod.rs"
      via: "crate::common::shell_quote(install_path) 转义 disql 命令"
      pattern: "shell_quote"
---

<objective>
新增 `dm-installer status` 子命令，查询本地 DM 实例与 config.toml 中所有远程节点的进程/端口/角色状态，输出对齐文本表格。

Purpose: 让 DBA / 开发者一行命令掌握集群运行状态，无需登录每台机器手动 ps + ss + disql。
Output: src/status/mod.rs（约 250 行含测试）+ src/cli.rs Status 变体 + src/main.rs dispatch 分支 + 全部单元测试通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-status/06-CONTEXT.md
@.planning/phases/06-status/06-RESEARCH.md
@.planning/phases/06-status/06-PATTERNS.md
@.planning/phases/06-status/06-VALIDATION.md

<interfaces>
<!-- 关键类型与签名 — 由规划者从代码库抽取，执行者无需再 grep -->

From src/common/ssh/runner.rs:
- pub trait CommandRunner: Send + Sync
  - async fn exec(&self, command: &str) -> Result<(Vec<u8>, u32), SshError>
  - async fn sftp_write(&self, remote_path: &str, bytes: &[u8]) -> Result<(), SshError>
  - async fn sftp_read(&self, remote_path: &str) -> Result<Vec<u8>, SshError>

From src/common/ssh/session.rs:
- pub struct SshSession
  - pub async fn connect(host: &str, port: u16, creds: &SshCredentials) -> Result<Self, SshError>
- 实现了 CommandRunner

From src/common/ssh/error.rs:
- pub enum SshError
  - Connect { host: String, source: russh::Error }
  - ExecFailed { command: String, exit_code: u32, output: String }
  - SftpUpload { ... }
  - SftpDownload { ... }

From src/common/ssh/mod.rs (re-exports):
- pub use error::SshError;
- pub use mock::MockRunner;
- pub use runner::CommandRunner;
- pub use session::SshSession;

From src/common/mod.rs:
- pub(crate) fn shell_quote(raw: &str) -> String  — 单引号转义，防 disql 命令注入

From src/config/ssh.rs:
- pub struct SshCredentials { pub user: String, /* identity_file, password */ ... }

From src/config/cluster.rs:
- pub enum NodeRole { Primary, Standby, Monitor }  — #[serde(rename_all = "lowercase")]
- pub struct NodeConfig {
    pub role: NodeRole,
    pub host: String,
    pub instance_name: String,
    pub mal_port: u16,
    pub dw_port: u16,
    pub inst_dw_port: u16,
    pub read_only: bool,
    pub ssh: SshCredentials,
  }
- pub struct DminitConfig {
    pub install_path: String,    // 默认 "/opt/dmdbms"（集群）
    pub port: u16,               // 默认 5236
    pub sysdba_password: String, // 默认 "SYSDBA"
    /* page_size, charset, ... */
  }
- pub struct ClusterSpecificConfig {
    pub oguid: u32,
    pub nodes: Vec<NodeConfig>,
    pub dminit: DminitConfig,
    /* mal, watcher, sqllog, dm_ini, archive */
  }

From src/config/mod.rs:
- pub enum LoadedConfig {
    Standalone { common: CommonConfig, specific: InstallConfig },
    Cluster { common: CommonConfig, specific: ClusterSpecificConfig, install_type: InstallType },
  }
- pub fn load_config() -> Result<LoadedConfig>  — 读 ./config.toml 自动判类型
- pub struct InstallConfig {
    pub install_path: String,    // 默认 "/home/dmdba/dmdbms"（单机）
    pub port: u16,               // 默认 5236
    /* instance_name, page_size, ... */
  }

From src/cli.rs (现有模式参照):
- #[derive(Subcommand)] pub enum Commands { Install, Validate, Init, Completions, /* 新增 Status */ }
- #[derive(clap::Args)] pub struct ValidateArgs { pub config: Option<PathBuf> }

From src/main.rs (现有 dispatch 模式):
- mod cli; mod cluster; mod common; mod config; mod guide; mod standalone;
- match &cli_args.command { cli::Commands::Validate(args) => config::validate::run(args).await, ... }

From src/cluster/preflight.rs (端口检测 + grep exit_code 1 + join_all 模式直接参考):
- async fn check_port_available(runner: &dyn CommandRunner, port: u16) -> Result<()>
  使用 `ss -tlnp | grep ':{port}'`，对 SshError::ExecFailed { exit_code: 1, .. } 显式处理为"无匹配 = 端口空闲"
- async fn preflight_all_nodes(items: Vec<(NodeConfig, Arc<dyn CommandRunner>)>, dminit: &DminitConfig) -> Result<()>
  使用 futures::future::join_all 并发，收集所有失败后统一 bail

From src/cluster/phases.rs:277-282 (disql 命令格式，已验证):
- 命令模板: "echo 'SELECT STATUS$,MODE$ FROM V$INSTANCE;' | {install_path}/bin/disql SYSDBA/{password}@localhost:{port}"
  其中 install_path 和 sysdba_password 必须用 shell_quote 转义。
- 输出格式: "STATUS$   MODE$\nOPEN      STANDBY\n" 或 "OPEN      PRIMARY\n"

From src/cluster/health.rs (TCP connect 模式):
- 使用 tokio::net::TcpStream::connect(addr).await + tokio::time::timeout 包裹

From src/common/ssh/mock.rs (测试 mock):
- MockRunner::new(vec![(cmd_prefix: String, exit_code: u32, stdout: Vec<u8>)])
  匹配命令前缀。exit_code != 0 时返回 SshError::ExecFailed。
- MockRunner::new_strict(...) — 未匹配命令返回 exit 127 Err
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 Status 子命令骨架 + 本地检测 + 单元测试 (STAT-01)</name>
  <files>src/cli.rs, src/main.rs, src/status/mod.rs</files>
  <read_first>
    - src/cli.rs (全文 — 看 ValidateArgs 风格、Commands 枚举、test_validate_defaults_to_no_path 测试模式)
    - src/main.rs (全文 — 看 mod 声明位置、match dispatch 模式)
    - src/common/mod.rs (确认 shell_quote 签名)
    - src/config/mod.rs:131-146 (LoadedConfig 枚举 + load_config 入口)
    - src/cluster/health.rs (TcpStream::connect + tokio::time::timeout 用法)
    - .planning/phases/06-status/06-CONTEXT.md (D-01, D-02, D-03, D-04, D-06, D-07, D-08, D-18)
    - .planning/phases/06-status/06-PATTERNS.md (段落 1-2 + 段落 6 本地端口检测)
  </read_first>
  <behavior>
    - test_status_args_parses: `Cli::try_parse_from(["dm-installer", "status"])` 解析为 `Commands::Status(StatusArgs { .. })`，无需任何参数
    - test_parse_role_from_disql_primary: 输入 "STATUS$   MODE$\nOPEN      PRIMARY\n" → 返回 "PRIMARY"
    - test_parse_role_from_disql_standby: 输入 "STATUS$   MODE$\nOPEN      STANDBY\n" → 返回 "STANDBY"
    - test_parse_role_from_disql_open_only: 输入 "STATUS$   MODE$\nOPEN      NORMAL\n" → 返回 "OPEN"
    - test_parse_role_from_disql_unknown: 输入 "ERROR: connection refused" → 返回 "unknown"
    - test_node_role_label: NodeRole::Primary → "primary"; NodeRole::Standby → "standby"; NodeRole::Monitor → "monitor"
    - test_check_local_port_closed: 对一个保证未监听的随机高端口（如 1）TcpStream::connect 在 1s 超时内返回 "closed"
  </behavior>
  <action>
    在 src/cli.rs 的 `Commands` 枚举（per D-01）追加变体 `Status(StatusArgs)`，doc comment 写为 "查询本地及 config.toml 中所有远程节点的运行状态"；在文件末尾（现有 InitOutputArgs 之后、tests 模块之前）新增 `#[derive(clap::Args)] pub struct StatusArgs {}`（无字段）。在 `#[cfg(test)] mod tests` 中追加 `test_status_args_parses`，断言子命令解析为 Status 变体。

    在 src/main.rs 第 6-11 行的 mod 声明区追加 `mod status;`（per D-03，按字母序插入到 standalone 上方）；在 match dispatch（约第 20-45 行）的 `cli::Commands::Completions { shell }` 分支之前追加 `cli::Commands::Status(args) => status::run(args).await,`（per D-02）。注意 Status 分支不能复用 Install 的 `unwrap_or_else + exit(1)` 模式，必须直接调用 status::run。

    新建文件 src/status/mod.rs。文件顶部导入：anyhow::Result, futures::future::join_all, tokio::time::{timeout, Duration}, std::time::Duration 不要混用（统一用 tokio::time::Duration），以及 crate::cli::StatusArgs, crate::common::shell_quote, crate::common::ssh::{CommandRunner, SshError, SshSession}, crate::config, crate::config::cluster::{ClusterSpecificConfig, NodeConfig, NodeRole}。

    定义类型：
    - `pub struct NodeStatus { pub node: String, pub host: String, pub process: String, pub port: String, pub role: String }` —— 五列对应 D-17 表头；error 节点用 process="—", port="—", role="ERROR: {原因}"（per D-15）。

    定义 helper 函数（全部 pub(crate) 或私有，便于测试）：
    - `fn parse_role_from_disql(output: &str) -> String` —— per D-08/D-12 决策：若含 "PRIMARY" 返回 "PRIMARY"；否则若同时含 "OPEN" 与 "STANDBY" 返回 "STANDBY"；否则若含 "OPEN" 返回 "OPEN"；否则返回 "unknown"
    - `fn node_role_label(role: NodeRole) -> &'static str` —— 显式 match 返回 "primary"/"standby"/"monitor"（per D-18 + RESEARCH Pitfall 5，不用 Debug）
    - `async fn check_local_port(port: u16) -> &'static str` —— `tokio::net::TcpStream::connect("127.0.0.1:{port}")` 套 `timeout(Duration::from_secs(1), ...)`，连通返回 "listening"，超时/拒绝返回 "closed"（per D-07 + 06-PATTERNS.md 段落 6）
    - `fn detect_local_process() -> &'static str` —— `std::process::Command::new("sh").arg("-c").arg("ps aux | grep dmserver | grep -v grep").output()`，OK 且 stdout 非空返回 "running"，否则返回 "stopped"（per D-06）
    - `async fn query_local_role(install_path: &str, sysdba_password: &str, port: u16) -> String` —— 仅当 port 已被本任务函数确认 listening 时由调用方触发；用 `std::process::Command::new("sh").arg("-c").arg(format!("echo '...' | {}/bin/disql SYSDBA/{}@localhost:{}", shell_quote(install_path), shell_quote(sysdba_password), port))`，exit 非 0 或解析失败返回 "unknown"，否则调用 parse_role_from_disql
    - `pub async fn run(_args: &StatusArgs) -> Result<()>` —— 顶层入口 stub：本任务先实现 no-config 与 standalone 两个分支（Cluster 分支由 Task 2/3 接管）。流程：(1) `let cfg = config::load_config().ok();` (2) `match cfg`：None → 用默认 install_path="/opt/dmdbms"、port=5236、password="SYSDBA"；Standalone → 用 specific.install_path/port + "SYSDBA"；Cluster → 调用占位函数 `query_cluster_nodes(specific).await`（本任务先返回空 Vec，Task 2 实现）。(3) 构造仅含 local 一行的 `Vec<NodeStatus>`，process 来自 detect_local_process()，port 来自 check_local_port()（listening/closed），role 来自 query_local_role()（仅当 port listening 时调用，否则 role="—"）。(4) 本任务先用 `println!("{:#?}", rows);` 占位输出（Task 3 替换为 format_table）。

    在文件末尾追加 `#[cfg(test)] mod tests { ... }`，写齐 behavior 中列出的 7 个测试。注意：test_check_local_port_closed 必须用 `#[tokio::test]`，端口选 `1`（可靠保证不监听）。

    在 Cargo.toml 检查 `[dependencies]` 段是否已含 `futures`；若已存在（preflight.rs 已用 join_all 应已声明）则不动；若不在则追加 `futures = "0.3"`。tokio 与 anyhow 必然已存在，无需改动。
  </action>
  <verify>
    <automated>cargo test -p dm-database-installer --lib cli::tests::test_status_args_parses status::tests -- --nocapture 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'Status\(StatusArgs\)' src/cli.rs` 命中至少 1 行（去除文件头注释行）
    - `grep -nE 'pub struct StatusArgs' src/cli.rs` 命中正好 1 行
    - `grep -nE 'mod status;' src/main.rs` 命中正好 1 行
    - `grep -nE 'Commands::Status\(args\)\s*=>\s*status::run\(args\)\.await' src/main.rs` 命中正好 1 行
    - `test -f src/status/mod.rs` 退出 0
    - `grep -nE 'pub async fn run\(_args: &amp;StatusArgs\)' src/status/mod.rs` 命中正好 1 行
    - `grep -nE 'fn parse_role_from_disql' src/status/mod.rs` 命中正好 1 行
    - `grep -nE 'fn node_role_label' src/status/mod.rs` 命中正好 1 行
    - `grep -nE 'fn detect_local_process' src/status/mod.rs` 命中正好 1 行
    - `grep -nE 'async fn check_local_port' src/status/mod.rs` 命中正好 1 行
    - `grep -nE 'shell_quote' src/status/mod.rs` 命中至少 1 行
    - `cargo test -p dm-database-installer --lib status::tests` 全绿（行尾应见 "test result: ok"，至少 7 个 passed）
    - `cargo test -p dm-database-installer --lib cli::tests::test_status_args_parses` 全绿
    - `cargo build` 无 warning 失败（允许已有 warning，但不能因本任务新引入 unused import）
  </acceptance_criteria>
  <done>
    Status 子命令可通过 clap 解析；本地状态检测函数（process / port / role 解析 / role label）单元测试全绿；`dm-installer status` 在 no-config 目录运行不 panic、退出码 0（即使输出仍是占位 Debug 格式）。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 集群远程节点 SSH 并发查询 + 错误降级 (STAT-02)</name>
  <files>src/status/mod.rs</files>
  <read_first>
    - src/status/mod.rs (Task 1 完成的当前文件，看 Task 1 已建立的类型与导入)
    - src/cluster/preflight.rs:23-127 (ss + grep + join_all 三个模式的精确实现)
    - src/cluster/phases.rs:270-310 (disql 命令构造与输出解析的实际调用)
    - src/common/ssh/mock.rs (MockRunner::new 测试 mock 模式 + new_strict 的语义)
    - src/common/ssh/error.rs (SshError 变体精确字段名)
    - src/config/cluster.rs:190-244 (NodeConfig + ClusterSpecificConfig 全字段)
    - .planning/phases/06-status/06-CONTEXT.md (D-09 ~ D-15)
    - .planning/phases/06-status/06-RESEARCH.md (Common Pitfalls 1-5)
  </read_first>
  <behavior>
    - test_check_remote_port_listening: MockRunner 返回 stdout="LISTEN 0 128 *:5236\n" exit=0 → 函数返回 "listening"
    - test_check_remote_port_closed_grep_exit1: MockRunner 让 ss|grep 返回 exit_code=1 stdout=空 → 函数返回 "closed"（per Pitfall 4）
    - test_check_remote_port_ss_missing_exit127: MockRunner 让命令返回 exit_code=127 → 函数返回 "unknown"（per Pitfall 1）
    - test_check_remote_process_running: MockRunner 返回 "dmdba 1234 .... dmserver path/dm.ini\n" → 函数返回 "running"
    - test_check_remote_process_stopped_grep_exit1: MockRunner ps|grep 返回 exit_code=1 → 函数返回 "stopped"
    - test_query_remote_role_primary: 三条命令依次返回 (ps running, ss listening, disql "OPEN PRIMARY") → 返回 Role="PRIMARY"
    - test_query_remote_role_skipped_when_port_closed: ps running + ss exit 1 → role="—"（per D-08/D-12：仅当 port listening 时才查 role），disql 命令未被调用（MockRunner.exec_log() 不含 disql 字符串）
    - test_query_remote_node_returns_error_on_disql_failure: ps running + ss listening + disql exit 非 0 → Role="unknown"，整条记录仍 process="running", port="listening"
    - test_query_cluster_nodes_concurrent_collects_all: 给两个节点的 MockRunner，第二个节点的 ps 返回 exit 1（stopped），结果 Vec 长度 = 2 且两节点的状态正确（验证 join_all 不因单节点失败丢弃结果）
  </behavior>
  <action>
    在 src/status/mod.rs 追加以下函数，并新增对应的 #[cfg(test)] mod tests 用例。所有函数接受 `runner: &amp;dyn CommandRunner`（trait 对象），既支持 SshSession 又支持 MockRunner（per Pattern 5 from preflight.rs）。

    - `async fn check_remote_port(runner: &amp;dyn CommandRunner, port: u16) -> &amp;'static str` —— per D-11 + Pitfall 4：构造 `format!("ss -tlnp | grep ':{port}'")`；match runner.exec(&amp;cmd).await：(stdout, _) 且 !stdout.is_empty() → "listening"；(stdout, _) 空 → "closed"；Err(SshError::ExecFailed { exit_code: 1, .. }) → "closed"；Err(SshError::ExecFailed { exit_code: 127, .. }) → "unknown"；其他 Err → "unknown"。
    - `async fn check_remote_process(runner: &amp;dyn CommandRunner) -> &amp;'static str` —— per D-10 + Pitfall 4：构造 `"ps aux | grep dmserver | grep -v grep"`；与上同模式：非空 → "running"；空或 exit_code 1 → "stopped"；其他 Err → "stopped"（per D-15 静默降级）。
    - `async fn query_remote_role(runner: &amp;dyn CommandRunner, install_path: &amp;str, sysdba_password: &amp;str, port: u16) -> String` —— per D-12 + phases.rs:277-282：构造命令 `format!("echo 'SELECT STATUS$,MODE$ FROM V$INSTANCE;' | {}/bin/disql SYSDBA/{}@localhost:{}", shell_quote(install_path), shell_quote(sysdba_password), port)`；match runner.exec(&amp;cmd).await：Ok((stdout, _)) → parse_role_from_disql(&amp;String::from_utf8_lossy(&amp;stdout))；Err(_) → "unknown"（per Pitfall 2 + D-15，disql 失败不传播）。
    - `async fn query_remote_node(node: &amp;NodeConfig, port: u16, install_path: &amp;str, sysdba_password: &amp;str) -> NodeStatus` —— 实际生产版本：调用 `timeout(Duration::from_secs(5), SshSession::connect(&amp;node.host, 22, &amp;node.ssh)).await`（per D-09 + Pitfall 3）；Err(_) timeout → `NodeStatus { node: node_role_label(node.role).to_string(), host: node.host.clone(), process: "—".into(), port: "—".into(), role: "ERROR: 连接超时".into() }`；Ok(Err(e)) → role: format!("ERROR: {}", e)；Ok(Ok(session)) → 调用 `query_remote_node_with_runner(&amp;session, node, port, install_path, sysdba_password).await`。
    - `async fn query_remote_node_with_runner(runner: &amp;dyn CommandRunner, node: &amp;NodeConfig, port: u16, install_path: &amp;str, sysdba_password: &amp;str) -> NodeStatus` —— 抽出此函数便于 MockRunner 测试：依次调用 check_remote_process、check_remote_port；若 port == "listening" 则调用 query_remote_role 取 role，否则 role 字符串置为 "—"；返回 NodeStatus { node: node_role_label(node.role).to_string(), host: node.host.clone(), process: process_str.to_string(), port: port_str.to_string(), role }。
    - `async fn query_cluster_nodes(cluster: &amp;ClusterSpecificConfig) -> Vec<NodeStatus>` —— per D-14 + preflight.rs:101-127：克隆 install_path/sysdba_password/port 进闭包；`let futures = cluster.nodes.iter().map(|node| { let node = node.clone(); let install_path = cluster.dminit.install_path.clone(); let password = cluster.dminit.sysdba_password.clone(); let port = cluster.dminit.port; async move { query_remote_node(&amp;node, port, &amp;install_path, &amp;password).await } });`；`join_all(futures).await`。

    在 Task 1 的 `pub async fn run` 中的 Cluster 分支替换占位调用：原 `query_cluster_nodes` 返回空 Vec 改为真正实现；将远程节点状态追加到 `Vec<NodeStatus>`（顺序：local 行第一，然后是 cluster.nodes 的并发结果）。

    单元测试：在 `mod tests` 中追加 behavior 中列出的 9 个测试，全部用 `MockRunner::new(vec![...])` 注入，使用 `query_remote_node_with_runner` 而非 `query_remote_node`（后者需要真实 SshSession::connect）。对 test_query_cluster_nodes_concurrent_collects_all，可直接调用 `query_remote_node_with_runner` 两次后构造 Vec 断言，无需真实 join_all（join_all 行为由 futures 库保证；本测试只需证明 query_remote_node_with_runner 对单节点失败返回 NodeStatus 而非 panic 即可）。
  </action>
  <verify>
    <automated>cargo test -p dm-database-installer --lib status::tests -- --nocapture 2>&amp;1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'async fn check_remote_port' src/status/mod.rs` 命中正好 1 行
    - `grep -nE 'async fn check_remote_process' src/status/mod.rs` 命中正好 1 行
    - `grep -nE 'async fn query_remote_role' src/status/mod.rs` 命中正好 1 行
    - `grep -nE 'async fn query_remote_node\b' src/status/mod.rs` 命中正好 1 行
    - `grep -nE 'async fn query_remote_node_with_runner' src/status/mod.rs` 命中正好 1 行
    - `grep -nE 'async fn query_cluster_nodes' src/status/mod.rs` 命中正好 1 行
    - `grep -nE 'join_all' src/status/mod.rs` 命中至少 1 行
    - `grep -nE 'SshError::ExecFailed \{ exit_code: 1' src/status/mod.rs` 命中至少 1 行（grep 无匹配处理）
    - `grep -nE 'SshError::ExecFailed \{ exit_code: 127' src/status/mod.rs` 命中至少 1 行（ss 缺失处理）
    - `grep -nE 'tokio::time::timeout|timeout\(Duration' src/status/mod.rs` 命中至少 1 行（SSH 超时）
    - `cargo test -p dm-database-installer --lib status::tests` 全绿（test result: ok. 至少 16 个 passed —— Task 1 的 7 个 + Task 2 的 9 个）
    - 在测试 `test_query_remote_role_skipped_when_port_closed` 中，断言 `runner.exec_log()` 不含 "disql" 子串（验证短路逻辑）
    - `cargo clippy -p dm-database-installer --lib -- -D warnings` 通过（无新增 clippy 错误）
  </acceptance_criteria>
  <done>
    远程节点 SSH 查询完整链路（连接 → ps → ss → disql）实现完成；单节点失败（连接超时、命令失败、grep 无匹配、ss 缺失、disql 失败）全部被静默降级为 NodeStatus，不污染整体结果；MockRunner 测试覆盖所有降级分支。Cluster 分支已与 Task 1 的 run() 入口集成（不再返回空 Vec）。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 对齐表格格式化输出 + 集成 (STAT-03)</name>
  <files>src/status/mod.rs</files>
  <read_first>
    - src/status/mod.rs (Task 1 + 2 完成后的当前文件)
    - .planning/phases/06-status/06-CONTEXT.md (D-16, D-17, D-18 + Claude's Discretion 列宽建议)
    - .planning/phases/06-status/06-RESEARCH.md (Pattern 5: 对齐表格格式化的假设示例)
  </read_first>
  <behavior>
    - test_format_table_minimum_widths: 输入仅 1 行 local 节点（host="localhost" 9 字符），输出第一行表头各列宽度 >= 默认最小宽度（Node>=7 "Node   ", Host>=14, Process>=7, Port>=4, Role>=7）
    - test_format_table_expands_for_long_host: 输入含 host="very-long-host.example.com" 的节点，host 列宽度恰好等于该 host 字符串长度（动态扩展）
    - test_format_table_three_columns_alignment: 三行输入（local + standby + monitor），输出按列对齐：用 split_whitespace + 列数 == 5 检测每行（含表头与分隔线之外的数据行）保持一致的字段数（5 个非空字段）
    - test_format_table_error_row: 含 process="—", port="—", role="ERROR: 连接超时" 的行；输出对应行的 role 列起始位置与其他行对齐（用 char index 计算）
    - test_format_table_separator_line: 表头之后必有一行由 "-" 组成的分隔线，长度等于表头行的总长度（去除尾换行后比较）
    - test_run_no_config_prints_local_only: 在临时目录无 config.toml 时调用 run()，捕获 stdout（用 println! → 不太能直接断言，所以本测试用 format_table 替代验证）—— 见 acceptance criteria
  </behavior>
  <action>
    在 src/status/mod.rs 追加表格格式化函数 `fn format_table(rows: &amp;[NodeStatus]) -> String`，per D-16 + Pattern 5 的动态列宽策略（Claude's Discretion）：

    - 固定最小宽度（per D-17 示例）：Node 最小 7（"Node   "），Host 最小 14（"Host          "），Process 最小 7（"Process"），Port 最小 4（"Port"），Role 最小 7（"-------"）。
    - 动态扩展：实际列宽 = max(最小宽度, 该列所有值的最大字符数)。
    - 用 format!("{:<width$}", value, width=col_width) 左对齐。
    - 列之间用 "  "（两个空格）分隔。
    - 表头之后插入分隔线：每列用 "-".repeat(col_width) 拼接，列分隔同样用 "  "。
    - 数据行：按 NodeStatus 字段顺序 (node, host, process, port, role) 输出。
    - 函数返回 String（包含尾部 "\n"），由 run() 用 print!() 输出（不用 println!，避免多余空行）。

    修改 `pub async fn run` 顶部入口：在收集完所有 NodeStatus 之后（local 一行 + cluster.nodes 的 Vec），调用 `let table = format_table(&amp;rows); print!("{}", table);`，移除 Task 1 占位的 `println!("{:#?}", rows);`。

    单元测试：在 `mod tests` 中追加 behavior 中列出的 5 个 format_table 测试 + 1 个 no_config 行为测试。
    - 对 test_format_table_separator_line：调用 format_table 后，按 '\n' split，取 lines[0] 长度 N，lines[1] 长度 == N 且 lines[1].chars().all(|c| c == '-' || c == ' ')。
    - 对 test_format_table_three_columns_alignment：可用 `lines[i].split("  ").filter(|s| !s.is_empty()).count() == 5` 验证 5 列。
    - test_run_no_config_prints_local_only 可省略 stdout 断言，转而验证：在 tempfile::tempdir 中切换 cwd（用 `std::env::set_current_dir` 但注意线程隔离），调用 run().await 返回 Ok(())。若线程切换 cwd 不安全（多个 #[tokio::test] 并行），改为：直接构造一个仅含 local 行的 Vec<NodeStatus> 然后调用 format_table，断言输出含 "local" 字符串且含 5 列表头。优先采用后者（无副作用）。
  </action>
  <verify>
    <automated>cargo test -p dm-database-installer --lib status -- --nocapture 2>&amp;1 | tail -60 &amp;&amp; cargo build --release 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'fn format_table' src/status/mod.rs` 命中正好 1 行
    - `grep -nE 'print!\(' src/status/mod.rs` 命中至少 1 行（替代 println!("{:#?}", ...)）
    - `grep -cnE 'println!\("\{:\#\?\}", rows\)' src/status/mod.rs` 输出为 0（Task 1 占位 Debug 输出已移除）
    - `cargo test -p dm-database-installer --lib status::tests` 全绿（test result: ok. 至少 21 个 passed —— 累计 Task 1+2+3 的测试，含 ≥6 个 format_table 相关）
    - `cargo test -p dm-database-installer` 全套绿（不引入新失败）
    - `cargo build --release` 成功，无 error
    - `cargo run --quiet --release -- status 2>&amp;1 | head -5` 在无 config.toml 的目录运行，前两行分别匹配正则：第 1 行 `^Node\s+Host\s+Process\s+Port\s+Role\s*$`；第 2 行 `^-+\s+-+\s+-+\s+-+\s+-+\s*$`
    - `cargo run --quiet --release -- status 2>&amp;1 | grep -E '^local\s+localhost\s+(running|stopped)\s+(listening|closed)' | wc -l` 输出 `1`（local 数据行存在且字段格式正确）
    - 退出码 0：`cargo run --quiet --release -- status; echo $?` 末行为 `0`
  </acceptance_criteria>
  <done>
    `dm-installer status` 在无 config 目录输出合法对齐表头 + local 行（process/port/role 真实数据）；表格列宽根据最长字段动态扩展；所有 21+ 个单元测试绿；cargo test 全套绿；cargo build --release 成功。STAT-01/02/03 三条需求均由代码 + 测试覆盖。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户 → status CLI | 用户在 shell 调用，传递无参数（StatusArgs 无字段），无注入面 |
| config.toml → status CLI | 从磁盘读取 config.toml，含 NodeConfig.host / install_path / sysdba_password；这些字段拼入 disql 命令字符串 |
| status CLI → 本地 shell（ps / disql）| 通过 `sh -c` 执行命令字符串，install_path / password 字段可控 |
| status CLI → 远程 SSH（ps / ss / disql）| 通过 CommandRunner::exec 执行命令字符串，同上 |
| 远程节点 stdout → status CLI | 远程节点回传 disql 输出，由 parse_role_from_disql 用 .contains() 解析（非 eval/exec），无注入面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | disql 命令构造（本地 + 远程）— install_path / sysdba_password 来自 config.toml | mitigate | 全部用 `crate::common::shell_quote()` 单引号转义后再拼入命令字符串，与 phases.rs:277-282 现有模式一致；测试覆盖含特殊字符的 install_path |
| T-06-02 | Tampering | ps / ss 命令构造 — port 来自 config.toml（u16 类型） | accept | port 为 u16 解析强类型，无字符串注入面；命令模板 hard-coded |
| T-06-03 | Information Disclosure | SYSDBA/SYSDBA 明文密码出现在 disql 命令行 | accept | 已知问题（CR-02 / D-13），Phase 6 不修复；命令仅在 SSH 加密通道内执行，本地命令在用户自己 shell 内 |
| T-06-04 | Denial of Service | SSH 连接到防火墙黑洞节点导致 status 永久挂起 | mitigate | tokio::time::timeout 5s 包裹 SshSession::connect（per D-09 + RESEARCH Pitfall 3） |
| T-06-05 | Spoofing | SSH 服务端公钥未验证（TOFU） | accept | 现有 TofuHandler 沿用（CR-01 已知问题），Phase 6 不变更 |
| T-06-06 | Repudiation | status 命令无审计日志 | accept | 只读查询，无副作用，无需审计；tracing::debug 日志已足够 |
| T-06-07 | Elevation of Privilege | status 命令本身不需 sudo（与 install 不同） | mitigate | run() 不调用 sudo；本地 disql 用当前用户身份运行（与 phases.rs 一致） |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | 本 phase 不新增 crate（futures 已在 dependencies），无供应链新引入；如需新增需运行 Package Legitimacy Gate |
</threat_model>

<verification>
- `cargo test -p dm-database-installer` 全套绿
- `cargo build --release` 成功
- `cargo run --quiet --release -- status` 在 no-config 目录输出对齐表头 + local 行（人工验证）
- `cargo clippy -p dm-database-installer --lib -- -D warnings` 通过
- 表头格式匹配 D-17 示例（Node/Host/Process/Port/Role 五列）
</verification>

<success_criteria>
- STAT-01: 本地进程 + 端口 + 角色检测函数全部存在且测试覆盖
- STAT-02: 远程节点 SSH 并发查询 + 5 种失败降级路径（连接超时、连接拒绝、ss 缺失、grep 无匹配、disql 失败）全部测试覆盖
- STAT-03: 对齐表格输出，含 5 列，动态列宽，分隔线长度正确
- 21+ 个 status 单元测试全绿
- cargo test 全套绿，cargo build --release 成功
- `dm-installer status` 退出码 0（即使 disql 不可达 / SSH 失败）
</success_criteria>

<output>
Create `.planning/phases/06-status/06-01-SUMMARY.md` when done
</output>
